{
  "name": "pixegon-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0 <23 || >=24.0.0 <25"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npm run check && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "extract": "node scripts/extract-artifact.mjs",
    "verify": "npm run build && node scripts/verify-build.mjs"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.9",
    "astro": "7.0.9",
    "typescript": "6.0.3"
  }
}
